# --- SYSTEM GENERATED ---
.DS_Store
Thumbs.db

# --- BUILD ARTIFACTS (Don't commit compiled code) ---
backend/build/
backend/.gradle/
frontend/dist/
frontend/node_modules/

# --- SECRETS & KEYS (CRITICAL) ---
# We keep a template (see below), but ignore the real file
backend/src/main/resources/application.properties
.env

# --- DATA & BACKUPS (Don't commit heavy binary files) ---
pitcraft-data/
archives/
*.zip
*.log

# --- IDE SETTINGS (Optional, keeps repo clean) ---
.idea/
.vscode/
*.iml
